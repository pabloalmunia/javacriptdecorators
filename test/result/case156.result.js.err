function decorator(value, context) {
  if ((context.kind === "method" || context.kind === "getter" || context.kind === "setter" || context.kind === "init-method" || context.kind === "init-getter" || context.kind === "init-setter") && context.isStatic) {
    return {
      set(...args) {
        console.log(`starting ${context.name} with arguments ${args.join(", ")}`);
        const ret = value(...args);
        console.log(`ending ${context.name}`);
        return ret;
      },
      initialize(v) {
        console.log("initialize class");
        this.test = 10;
      }
    };
  }
}

if (!Symbol.metadata) {
  Symbol.metadata = Symbol();
}

function __DefineMetadata(base, name) {
  return function(key, value) {
    if (!base[Symbol.metadata]) {
      base[Symbol.metadata] = Object.create(null);
    }
    if (!base[Symbol.metadata][name]) {
      base[Symbol.metadata][name] = {};
    }
    const db = base[Symbol.metadata][name];
    if (key in db) {
      if (!Array.isArray(db[key])) {
        return db[key] = [db[key], value];
      }
      return db[key].push(value);
    }
    return db[key] = value;
  };
}

const _C_P_symbol_ojm51o = Symbol();

class C {
  static #other = 0;
  static get #P() {
    return C.#other;
  }
  static _C_P_temp_v8q03o(v) {
    return C.#other = v;
  }
  static [_C_P_symbol_ojm51o] = decorator(C._C_P_temp_v8q03o, {
    kind: "setter",
    name: "#P",
    isStatic: true,
    isPrivate: true,
    access: {
      get: C[_C_P_symbol_ojm51o]
    },
    defineMetadata: __DefineMetadata(C, "#P")
  }) ?? C._C_P_temp_v8q03o;
  static set #P(v) {
    return C[_C_P_symbol_ojm51o].bind(this)(v);
  }
  static [_C_P_symbol_ojm51o]() {
    return C[_C_P_symbol_ojm51o].bind(this);
  }
  static get check() {
    return C.#P;
  }
  static set check(v) {
    return C.#P = v;
  }
}

delete C._C_P_temp_v8q03o;

console.assert(C.check === 0);

C.check = 20;

console.assert(C.check === 20);

console.assert(C.test === 10);