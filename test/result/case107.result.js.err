function decorator(value, context) {
  console.log("value", value);
  console.log("context", context);
  return {
    get() {
      return value.call(this) * 2;
    },
    initialize() {
      this.test = 10;
    }
  };
}

if (!Symbol.metadata) {
  Symbol.metadata = Symbol();
}

function __DefineMetadata(base, name) {
  return function(key, value) {
    if (!base[Symbol.metadata]) {
      base[Symbol.metadata] = Object.create(null);
    }
    if (!base[Symbol.metadata][name]) {
      base[Symbol.metadata][name] = {};
    }
    const db = base[Symbol.metadata][name];
    if (key in db) {
      if (!Array.isArray(db[key])) {
        return db[key] = [db[key], value];
      }
      return db[key].push(value);
    }
    return db[key] = value;
  };
}

const _C_p_symbol_dedmag = Symbol();

class C {
  #other = 10;
  _C_p_temp_jrb8no() {
    return this.#other;
  }
  static [_C_p_symbol_dedmag] = decorator(C.prototype._C_p_temp_jrb8no, {
    kind: "getter",
    name: "#p",
    isStatic: false,
    isPrivate: true,
    access: {
      get: C.prototype[_C_p_symbol_dedmag]
    },
    defineMetadata: __DefineMetadata(C.prototype, "#p")
  }) ?? C.prototype._C_p_temp_jrb8no;
  get #p() {
    return C[_C_p_symbol_dedmag].bind(this)();
  }
  [_C_p_symbol_dedmag]() {
    return C[_C_p_symbol_dedmag].bind(this);
  }
  check() {
    return this.#p;
  }
}

delete C.prototype._C_p_temp_jrb8no;

console.assert(new C().test === 10);

console.assert(new C().check() === 20);