function decorator(value, context) {
  console.log("value", value);
  console.log("context", context);
  return {
    set(v) {
      value.call(this, v * 2);
    },
    initialize() {
      this.test = 10;
    }
  };
}

if (!Symbol.metadata) {
  Symbol.metadata = Symbol();
}

function __DefineMetadata(base, name) {
  return function(key, value) {
    if (!base[Symbol.metadata]) {
      base[Symbol.metadata] = Object.create(null);
    }
    if (!base[Symbol.metadata][name]) {
      base[Symbol.metadata][name] = {};
    }
    const db = base[Symbol.metadata][name];
    if (key in db) {
      if (!Array.isArray(db[key])) {
        return db[key] = [db[key], value];
      }
      return db[key].push(value);
    }
    return db[key] = value;
  };
}

const _C_p_symbol_iap0c8 = Symbol();

class C {
  #other = 10;
  get #p() {
    return this.#other;
  }
  _C_p_temp_1pmhp(v) {
    this.#other = v;
  }
  static [_C_p_symbol_iap0c8] = decorator(C.prototype._C_p_temp_1pmhp, {
    kind: "setter",
    name: "#p",
    isStatic: false,
    isPrivate: true,
    access: {
      get: C.prototype[_C_p_symbol_iap0c8]
    },
    defineMetadata: __DefineMetadata(C.prototype, "#p")
  }) ?? C.prototype._C_p_temp_1pmhp;
  set #p(v) {
    return C[_C_p_symbol_iap0c8].bind(this)(v);
  }
  [_C_p_symbol_iap0c8]() {
    return C[_C_p_symbol_iap0c8].bind(this);
  }
  set check(v) {
    this.#p = v;
  }
  get check() {
    return this.#p;
  }
}

delete C.prototype._C_p_temp_1pmhp;

console.assert(new C().test === 10);

const c = new C();

c.check = 20;

console.assert(c.check === 40);